import { FilterStoresByLetterPipe } from './filter-stores-by-letter.pipe';

describe('FilterStoresByLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStoresByLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
